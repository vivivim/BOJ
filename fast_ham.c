#include <stdio.h>

int	que[10][200000];

void	into_que(int ham, int j)
{
	int	a = 0, b = 0;

	if (j == 0)
	{
		while (que[j][a])
			a++;
		que[j][a] == 
	while (que[i])
	{


int	*sorting(int ham[], int n)
{
	for (int i = 0; i < 10; i++)
	{
		if (i == 0)
		{
			for (int j = 0; j < n; j++)
				into_que(ham[j], j);

}
int	 main(void)
{
	int	n, k l;
	scanf("%d %d %d", &n, &k, &l);

	int	ham[n];
	for (int i = 0; i < n; i++)
		scanf("%d", &scanf[i]);
	int	coke[k];
	for (int i = 0; i < k; i++)
		scanf("%d", &coke[i]);

	sorting(ham);

	return (0);
}
